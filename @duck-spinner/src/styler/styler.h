#ifndef COLOR_TEXT_H
#define COLOR_TEXT_H

// Define a max buffer size for the returned colored text
#define MAX_COLOR_TEXT 256

// Function prototype
const char *get_colored_text(const char *color, const char *text);

#endif // COLOR_TEXT_H
